export default function HomePage() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "2rem" }}>
      <h1>StackSpend</h1>
      <p>Local-first cloud/SaaS usage, status, and expected billing dashboard.</p>
      <section>
        <h2>v0.1 dashboard skeleton</h2>
        <p>Run the CLI sync/report pipeline before dashboard data is available.</p>
      </section>
    </main>
  );
}
